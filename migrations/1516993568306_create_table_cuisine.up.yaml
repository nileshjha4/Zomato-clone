- args:
    sql: CREATE TABLE "cuisine"("cuisine_id" serial NOT NULL, "restaurant_id" integer
      NOT NULL, "cuisine_name" text NOT NULL, PRIMARY KEY ("cuisine_id","restaurant_id")
      )
  type: run_sql
- args:
    name: cuisine
  type: add_existing_table_or_view
